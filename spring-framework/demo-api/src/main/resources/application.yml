spring.datasource:
    url: jdbc:mysql://localhost:3306/demoapidb
    username: usertodo
    password: Usertodo16$
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: update
        dialect: org.hibernate.dialect.MySQL8Dialect
logging:
    level:
      org:
        hibernate:
          type: trace
